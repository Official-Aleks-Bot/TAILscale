name: FreeRDP

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 9999

    steps:
    - name: Run Tailscale
      run: curl -fsSL https://tailscale.com/install.sh | sh -s -- --auth-key=$Env:APIKEY --advertise-exit-node
      env:
        APIKEY: ${{ secrets.APIKEY }}

    - name: loop
      run: |
        while true; do
          sleep 60
        done
